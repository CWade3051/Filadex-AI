# Database Configuration
POSTGRES_USER=filadex
POSTGRES_PASSWORD=filadex
POSTGRES_DB=filadex
PGHOST=db
PGPORT=5432

# Application Configuration
APP_PORT=8080
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARN, ERROR

# Admin User Configuration
ADMIN_PASSWORD=admin

# AI Features (Optional)
# You can set this globally here OR per-user in the app settings
OPENAI_API_KEY=

# Docker Mobile Upload Support
# REQUIRED for QR code mobile uploads to work in Docker
# Set this to your host machine's local network IP (e.g., 192.168.1.100)
# Find your IP: macOS: ifconfig | grep "inet " | Windows: ipconfig
HOST_IP=
